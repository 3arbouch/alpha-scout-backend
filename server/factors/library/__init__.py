"""Feature definitions, organized by category.

Each module's import has the side-effect of registering its features into
the shared registry. server/factors/__init__.py imports them all on package
load.
"""
