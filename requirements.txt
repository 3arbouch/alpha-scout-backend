fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
matplotlib>=3.7.0
anthropic>=0.18.0
# >=0.2.88 ships Claude CLI 2.1.161, which supports Opus 4.8's adaptive thinking.
# Earlier bundles (e.g. 0.1.61's CLI 2.1.112) predate Opus 4.8 and still emit the
# removed thinking.type=enabled shape for it, getting 400s from the API.
claude-agent-sdk>=0.2.88
PyJWT>=2.8.0
bcrypt>=4.0.0
numpy>=1.26
scipy>=1.10
