fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
matplotlib>=3.7.0
anthropic>=0.18.0
# >=0.1.61 ships Claude CLI 2.1.112, which supports Opus 4.7's adaptive thinking.
# Older SDK bundles CLI 2.1.97 which still sends the removed thinking.type=enabled
# shape and gets 400s from Opus 4.7.
claude-agent-sdk>=0.1.61
PyJWT>=2.8.0
bcrypt>=4.0.0
